package com.venegas.backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication //define a la clase como un componente cuando se instancia
public class BackendVenegasApplication {

	public static void main(String[] args) {
		SpringApplication.run(BackendVenegasApplication.class, args);
	}

}
